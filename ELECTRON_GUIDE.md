# 📖 คู่มือการติดตั้งและใช้งานระบบ RPA Promotion A_A (เวอร์ชันแอปพลิเคชัน)

> อัปเดตล่าสุด: พฤษภาคม 2026
> รูปแบบ: Windows Desktop Application (.exe)

---

## ⚙️ ความต้องการของระบบ (System Requirements)

| รายการ | รายละเอียด |
|--------|-----------|
| **ระบบปฏิบัติการ** | Windows 10 หรือ Windows 11 |
| **โปรแกรมที่ต้องติดตั้ง** | **ไม่ต้องติดตั้งโปรแกรมเสริมใดๆ!** (ไม่จำเป็นต้องใช้ Node.js) |
| **เบราว์เซอร์** | Google Chrome หรือ Microsoft Edge (ต้องมีอยู่ในเครื่อง) |
| **อินเทอร์เน็ต** | จำเป็นสำหรับการเชื่อมต่อ ShopAt24 และ Google Sheet |

---

## 🚀 การติดตั้ง (ทำครั้งเดียว)

เนื่องจากระบบถูกพัฒนาขึ้นมาเป็น **แอปพลิเคชันสำเร็จรูป** การติดตั้งจึงง่ายเหมือนการลงโปรแกรมทั่วไปครับ:

1. **รับไฟล์ติดตั้ง**: รับไฟล์ `RPA Promotion Setup 1.0.0.exe` จากทีมงาน IT (หรือดาวน์โหลดจากลิงก์ที่กำหนด)
2. **ทำการติดตั้ง**: ดับเบิลคลิกไฟล์ `RPA Promotion Setup 1.0.0.exe`
3. **รอสักครู่**: โปรแกรมจะทำการติดตั้งลงเครื่องของคุณโดยอัตโนมัติ (ใช้เวลาไม่เกิน 1 นาที)
4. **เสร็จสิ้น**: เมื่อติดตั้งเสร็จ จะมีไอคอนโปรแกรม **"RPA Promotion"** ปรากฏขึ้นบนหน้าจอ Desktop ของคุณครับ

---

## 📅 การใช้งานรายวัน

### 1. การเปิดใช้งานระบบ
1. ดับเบิลคลิกที่ไอคอน **"RPA Promotion"** บนหน้าจอ Desktop
2. หน้าต่างแอปพลิเคชัน (Dashboard) จะเปิดขึ้นมาทันที (จะไม่มีหน้าต่างสีดำเด้งขึ้นมากวนใจอีกต่อไป)

### 2. การสั่งให้โรบอททำงาน
1. ตรวจสอบข้อมูลใน Google Sheet ให้เรียบร้อย (ตั้งค่า Column J = Yes และ Column I ว่างหรือ Edit)
2. บนหน้า Dashboard:
   - ตรวจสอบ **Start Row** (แนะนำค่าปกติคือ 2 เพื่อข้าม Header)
   - เลือกรูปแบบหน้าต่างการทำงาน (**Off screen** ซ่อนหน้าต่าง หรือ **On screen** เปิดหน้าต่างให้เห็น)
3. กดปุ่ม **"เริ่มทำงาน"**
4. ระบบจะทำการค้นหา Chrome หรือ Edge ในเครื่องของคุณ และเริ่มทำงานให้โดยอัตโนมัติ 
5. คุณสามารถดูความคืบหน้าได้จาก Progress Bar และ System Log บนหน้าจอครับ

---

## 🔄 ระบบการอัปเดตอัตโนมัติ (Auto Update)

หากทีมงาน IT มีการปล่อยเวอร์ชันใหม่ (Patch) คุณไม่ต้องลบโปรแกรมทิ้งเพื่อลงใหม่ครับ:
1. ทันทีที่คุณเปิดโปรแกรมขึ้นมา หากมีอัปเดตใหม่ ระบบจะทำการดาวน์โหลดอยู่เบื้องหลังเงียบๆ
2. เมื่อโหลดเสร็จ จะมีหน้าต่างแจ้งเตือนขึ้นมาว่า **"มีอัปเดตใหม่พร้อมติดตั้ง"**
3. ให้คุณกดปุ่ม **Restart Now** โปรแกรมจะปิดและเปิดขึ้นมาใหม่กลายเป็นเวอร์ชันล่าสุดทันทีครับ!

---

## ❓ แก้ปัญหาเบื้องต้น (Troubleshooting)

| ปัญหา | วิธีแก้ |
|-------|--------|
| โปรแกรมฟ้องว่าหา Browser ไม่เจอ | กรุณาตรวจสอบว่าในเครื่องมี Google Chrome หรือ Microsoft Edge ติดตั้งอยู่หรือไม่ |
| บอท Login ไม่ผ่าน | โปรดแจ้งทีมงาน IT เพื่อตรวจสอบรหัสผ่านในไฟล์ระบบส่วนกลาง |
| โปรแกรมค้างหรือไม่ตอบสนอง | กดปุ่มกากบาท (X) มุมขวาบนเพื่อปิดโปรแกรม และเปิดใหม่ |

---

## 📞 ติดต่อ IT
หากพบปัญหาที่แก้ไขไม่ได้ โปรดแคปเจอร์หน้าจอ (Screenshot) ที่แสดง Error ใน System Log ส่งให้ทีมงาน IT เพื่อตรวจสอบครับ
